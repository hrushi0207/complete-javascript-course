'use strict';

// DEBUGGING ......CODE IS JUST FOR PRACTICE.....DOES NOT BELONG TO DEBUGGING

// let t1 = [12, 6, 5, 7];
// let t2 = [16, 3, 2, 4];

// const calTempAltitude = function (t1, t2) {
//   const temps = t1.concat(t2);
//   console.log([temps]);

//   let max = temps[0];
//   let min = temps[0];

//   for (let i = 0; i < temps.length - 1; i++) {
//     if (typeof temps[i] !== 'number') continue;

//     if (temps[i] > max) max = temps[i];
//     if (temps[i] < min) min = temps[i];
//   }
//   console.log(max, min);
//   return max - min;
// };
// const result = calTempAltitude(t1, t2);
// console.log(result);
//

// let arr = [17, 21, 23];

// const printForecaste = function (arr) {
//   let str = '';
//   for (let i = 0; i < arr.length; i++) {
//     str = str + `${arr[i]}C in ${i + 1}days...`;
//   }
//   console.log('...' + str);
// };

// console.log(printForecaste(arr));
